# Splicer WebContainer - Environment Variables
# Copy this file to .env and fill in your values
#
# For local development:
#   cp .env.example .env
#
# For production (Cloud Run):
#   Set these as secrets in GCP Secret Manager

# =============================================================================
# SUPABASE
# =============================================================================
# Your Supabase project URL
SUPABASE_URL=https://your-project.supabase.co

# Supabase SERVICE ROLE key (server-side only, never expose to clients)
# This bypasses Row Level Security - handle with care
SUPABASE_SERVICE_KEY=

# =============================================================================
# API SECURITY
# =============================================================================
# API key for authenticating requests from Edge Functions
# Generate a secure random string: openssl rand -base64 32
API_KEY=

# =============================================================================
# SERVER (Optional)
# =============================================================================
# Port for the FastAPI server (default: 8080)
PORT=8080

# Base URL for the preview service
# In production, this is your Cloud Run URL or load balancer
PREVIEW_BASE_URL=http://localhost:8080

# =============================================================================
# GITHUB (Optional - for local development)
# =============================================================================
# Personal Access Token for cloning repos locally
# Production uses installation tokens from Edge Functions
GITHUB_TOKEN=

# =============================================================================
# WORKSPACE
# =============================================================================
# Directory for preview workspaces (default: ./workspaces)
WORKSPACE_DIR=./workspaces

# Maximum workspaces to keep (for cleanup)
MAX_WORKSPACES=50
